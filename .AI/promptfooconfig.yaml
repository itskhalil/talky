description: 'enhance_notes — baseline (clean-all promoted Round 22)'

prompts:
  - id: file://prompts/baseline.mjs
    label: baseline

providers:
  - file://providers/talky.mjs

tests: file://tests/cases.mjs

defaultTest:
  assert:
    - type: javascript
      value: file://scorers/judge.mjs
