description: "enhance_notes — baseline (post Round 23: canonical-example promoted)"

prompts:
  - id: file://prompts/baseline.mjs
    label: baseline

providers:
  # Cache-first. For variance runs (promptfoo --repeat N), swap to
  # file://providers/talky-fresh.mjs so each repeat is a real API call.
  - file://providers/talky.mjs

tests: file://tests/cases.mjs

defaultTest:
  assert:
    - type: javascript
      value: file://scorers/judge.mjs
